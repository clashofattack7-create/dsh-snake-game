/**
 * dsh-client-ui-snake-game — node half.
 * The browser half ships through exports["./client"]; this row exists only so
 * the Loader has a host-side entry to scan for the `dsh.client` declaration.
 */
export function apply() {}
